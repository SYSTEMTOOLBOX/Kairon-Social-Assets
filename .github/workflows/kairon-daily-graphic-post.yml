name: Kairon Daily Graphic Post - DISABLED

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Old static graphic-post automation disabled. Kairon now uses premium Reels only."
